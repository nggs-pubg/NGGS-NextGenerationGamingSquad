# WebRTC

O navegador deve criar `RTCPeerConnection` usando `STUN_SERVER`, trocar offer/answer/ICE pelo WebSocket e adicionar tracks de `getUserMedia` ou `getDisplayMedia`. TURN é recomendado para redes restritivas.

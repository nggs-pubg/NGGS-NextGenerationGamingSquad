# Deploy

Configure os secrets `DEPLOY_HOST`, `DEPLOY_USER` e `DEPLOY_SSH_KEY`. O workflow manual atualiza o checkout do servidor e reconstrói o Compose. Use HTTPS no proxy e um segredo JWT aleatório em produção.
